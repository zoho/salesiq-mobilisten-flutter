.dart_tool/
.idea/
.vscode/
build/

# Android assets fonts — not needed in the repo (unreferenced; SDK provides its own Roboto,
# and pubspec declares no custom fonts). Kept locally, excluded from commits.
example/android/app/src/main/assets/fonts/
**/.DS_Store
